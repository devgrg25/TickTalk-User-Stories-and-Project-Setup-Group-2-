name: ticktalk_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.1.0
#  vibration: ^1.10.0
  flutter:
    sdk: flutter

  # UI & Fonts
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8

  # App state & storage
  shared_preferences: ^2.2.3
  connectivity_plus: ^6.0.3

  # Voice (Google Speech & Text-to-Speech)
  speech_to_text: ^7.0.0
  flutter_tts: ^3.8.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
